name: flutter_hw32
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  bloc: ^9.0.0
  firebase_core: ^4.6.0
  firebase_auth: ^5.3.4
  get_it: ^8.0.3
  google_sign_in: ^6.2.2
  provider: ^6.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
